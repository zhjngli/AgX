# Preset format reference

This page is generated from the `Preset` serde types by the `agx-docgen` tool, which is built in a separate sub-project. See the [documentation initiative](https://github.com/zhjngli/AgX/blob/main/docs/plans/2026-04-06-documentation-initiative-design.md) for the planned content and timeline.

In the meantime, the canonical example preset lives at `crates/agx-e2e/presets/` and the `Preset` type is defined in `crates/agx/src/preset/`.
