# CLI reference

This page is generated from the CLI's `clap::Command` definitions by the `agx-docgen` tool, which is built in a separate sub-project. See the [documentation initiative](https://github.com/zhjngli/AgX/blob/main/docs/plans/2026-04-06-documentation-initiative-design.md) for the planned content and timeline.

In the meantime, run `agx --help` for the full CLI reference.
